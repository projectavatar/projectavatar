name = "project-avatar-relay"
main = "src/index.ts"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]

[[durable_objects.bindings]]
name = "CHANNEL"
class_name = "Channel"

[[migrations]]
tag = "v1"
new_sqlite_classes = ["Channel"]

# KV removed — rate limiting moved to Durable Object SQL storage

[vars]
RELAY_VERSION = "1.0.0"
